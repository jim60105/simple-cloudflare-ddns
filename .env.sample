# Cloudflare API Configuration
# Get your API token from: https://dash.cloudflare.com/profile/api-tokens
API_TOKEN=your_cloudflare_api_token_here

# Zone ID from Cloudflare dashboard
# Found in the domain overview page
ZONE_ID=your_zone_id_here

# IPv4 (A Record) Configuration (optional)
# Get record ID using: curl -X GET "https://api.cloudflare.com/client/v4/zones/YOUR_ZONE_ID/dns_records/" -H "Authorization: Bearer YOUR_API_TOKEN" | jq '.result[] | {id, name, type}'
A_RECORD_ID=your_a_record_id_here
A_RECORD_NAME=home.example.com

# IPv6 (AAAA Record) Configuration (optional)
# Uncomment if you want to update IPv6 records
#AAAA_RECORD_ID=your_aaaa_record_id_here
#AAAA_RECORD_NAME=home.example.com

# IP Detection Services (optional)
# Uncomment to use alternative IP detection services
#IPV4_API_URL=https://api.ipify.org
#IPV6_API_URL=https://api6.ipify.org
